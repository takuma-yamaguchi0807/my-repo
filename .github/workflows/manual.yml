name: Manual
on:
  workflow_dispatch:
    inputs:
      greeting:
        type: string
        default: "Hello, World!"
        required: true
        description: "The greeting message to display"
jobs:
  run-manual:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ inputs.greeting }}"